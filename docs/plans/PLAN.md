# DiagramCore — план реалізації

> Виконуваний план для агентів. Кожен крок має **Acceptance Criteria (AC)** — механічно перевірювані умови.
> Правила виконання: кроки виконуються строго послідовно в межах фази; крок вважається завершеним лише коли ВСІ його AC проходять; після кожного кроку — commit з повідомленням `phase<N>-step<M>: <опис>`.

## Загальні рішення (зафіксовано, не переглядати без згоди власника)

- **Формат ядра:** YAML (`*.dc.yaml`) — мовно-незалежне джерело правди.
- **CLI:** Go (модуль `github.com/oleksii94/diagramcore`, бінарник `dc`). Go ≥ 1.22.
- **Рендеринг фази 3:** нативно через Go-бібліотеку D2 (`oss.terrastruct.com/d2`) + текстова транспіляція в Mermaid.
- **Layout зберігається окремо** від ядра: `<name>.layout.json`.
- **Flow ходить тільки по існуючих ребрах** — крок без відповідного link = помилка валідації.
- **Web-редактор (фаза 5):** TypeScript, окремий застосунок; валідація через Go→WASM.
- **Движок канви (фаза 6+):** React Flow (`@xyflow/react`, MIT), НЕ draw.io/mxGraph — YAML лишається джерелом правди, канва — проєкція; візуальне редагування генерує патчі до YAML, дії поза виразністю формату неможливі в UI.
- **Редагування YAML у web:** через бібліотеку `yaml` (eemeli/yaml) з CST-патчингом — коментарі та порядок ключів користувача зберігаються при візуальних правках.
- **MCP-сервер (фаза 9):** Go, всередині того ж бінарника (`dc mcp`, stdio-транспорт), через офіційний Go MCP SDK; редагування діаграм агентом — тільки структурованими операціями, не перезаписом файлу.
- Мова коду/ідентифікаторів — англійська; повідомлення CLI — англійська; приклади діаграм можуть бути українською.

## Структура репозиторію (цільова)

```
diagramcore/
├── PLAN.md
├── go.mod
├── cmd/dc/main.go            # CLI entrypoint (cobra або stdlib flag)
├── internal/
│   ├── model/                # структури ядра: Diagram, Node, Link, Flow
│   ├── parser/               # YAML → model
│   ├── validate/             # правила валідації
│   ├── context/              # генератор AI-контексту
│   ├── transpile/            # → D2, → Mermaid
│   └── render/               # виклик D2-бібліотеки, SVG/PNG, steps
├── schema/
│   └── diagramcore.schema.json   # JSON Schema формату (для редакторів/CI)
├── examples/
│   ├── auth-system.dc.yaml
│   ├── oauth-detail.dc.yaml
│   └── <ще 1-2 реальні діаграми>
├── testdata/                 # валідні та невалідні фікстури для тестів
└── web/                      # фаза 5 (TypeScript)
```

---

# Фаза 0 — Spike: формат на реальних прикладах

**Мета:** зафіксувати v0 формату YAML до написання коду.

### Крок 0.1 — Ініціалізація репозиторію
- `git init`, `go mod init github.com/oleksii94/diagramcore`, `.gitignore` (Go + node), скопіювати концепт-документ у `docs/concept.md`, цей план у корінь.
- **AC:**
  - [ ] `git log` містить initial commit.
  - [ ] `go build ./...` завершується без помилок (порожній модуль — ок).

### Крок 0.2 — Специфікація формату v0
Створити `docs/format.md`, який визначає структуру `*.dc.yaml`:

```yaml
diagram:
  title: "Система авторизації"
  purpose: "..."
  audience: students
  version: "0.1"

nodes:
  - id: User            # унікальний, [A-Za-z][A-Za-z0-9_]*
    type: actor         # з дозволеного набору типів
    label: "Користувач" # опційно, default = id
    description: "..."
    ai_context: "..."   # опційно
    tags: [auth]        # опційно
    details: ./oauth-detail.dc.yaml   # опційно, референс на під-діаграму

links:
  - from: User
    to: Gateway
    type: request       # request|call|query|event|dataflow|inherits|contains
    label: "HTTPS запит"
    directed: true      # default true

flows:
  - name: "Успішна авторизація"
    steps:
      - from: User
        to: Gateway
        note: "Надсилає запит з Bearer токеном"
      - branch:                      # умовна гілка
          condition: "токен валідний"
          then: [ {from: AuthService, to: DB, note: "..."} ]
          else: [ {from: AuthService, to: ErrorHandler, note: "..."} ]
```

Базові типи вузлів v0: `actor, service, storage, queue, external, component`. Розширення типів — поле `custom_types` на рівні diagram (описати в format.md, реалізація у фазі 1).
- **AC:**
  - [ ] `docs/format.md` описує всі секції (diagram, nodes, links, flows), кожне поле має тип, обов'язковість, default.
  - [ ] Описано семантику flow: крок валідний лише якщо існує link між from/to (для directed — у правильному напрямку; зворотний хід по directed-ребру дозволений і трактується як "відповідь/response").
  - [ ] Описано формат референсу на під-діаграму та правило: шлях відносний до поточного файлу.

### Крок 0.3 — Три реальні приклади
Написати вручну `examples/auth-system.dc.yaml` (з концепту, включно з обома flow), `examples/oauth-detail.dc.yaml` (під-діаграма) і ще один приклад іншого домену (напр. обробка платежу з branch).
- **AC:**
  - [ ] 3+ файли в `examples/`, кожен з ≥4 вузлами; `auth-system` має ≥2 flows; хоча б один приклад використовує `branch`; хоча б один — `details`-референс.
  - [ ] Кожен приклад — синтаксично валідний YAML (`python3 -c "import yaml,sys; yaml.safe_load(open(sys.argv[1]))" <file>` або будь-який YAML-лінтер проходить).
  - [ ] Приклади відповідають `docs/format.md` (ручна перевірка агентом: жодного поля, не описаного у форматі).

### Крок 0.4 — JSON Schema
Створити `schema/diagramcore.schema.json` (JSON Schema draft 2020-12), що описує формат.
- **AC:**
  - [ ] Всі файли з `examples/` проходять валідацію схемою (перевірити конвертацією YAML→JSON + будь-яким validator, напр. `check-jsonschema`).
  - [ ] Навмисно зіпсований файл (відсутнє `nodes[].id`) валідацію НЕ проходить.

---

# Фаза 1 — Ядро-бібліотека та `dc validate`

**Мета:** CLI, що парсить і семантично валідує діаграми. JSON Schema ловить структуру; Go-код ловить семантику.

### Крок 1.1 — Модель і парсер
`internal/model`: структури `Diagram, Node, Link, Flow, Step, Branch`. `internal/parser`: `Parse(path) (*Diagram, error)` через `gopkg.in/yaml.v3`, зі збереженням номерів рядків для повідомлень про помилки (yaml.Node).
- **AC:**
  - [ ] Unit-тести: парсинг усіх файлів з `examples/` повертає модель без помилок; поля вибірково звірені (кількість вузлів, назви flows).
  - [ ] Парсинг файлу з синтаксичною помилкою YAML повертає помилку з номером рядка.
  - [ ] `go test ./... && go vet ./...` — зелені.

### Крок 1.2 — Семантична валідація
`internal/validate` з правилами (кожне — окрема функція, окремий код помилки `DCxxx`):
- `DC001` дублікат node id; `DC002` link на неіснуючий вузол; `DC003` невідомий тип вузла/зв'язку (з урахуванням custom_types); `DC004` flow-крок між вузлами без link; `DC005` flow-крок на неіснуючий вузол; `DC006` `details`-файл не існує або сам не парситься; `DC007` порожній flow; `DC008` branch без then.
Результат — список помилок (усі одразу, не fail-fast), кожна з file:line.
Обхід `details`: валідатор рекурсивно перевіряє під-діаграми з множиною відвіданих файлів (канонічний абсолютний шлях) — кожен файл валідується рівно один раз; циклічні референси не є помилкою і не спричиняють нескінченний обхід (див. `docs/format.md`).
- **AC:**
  - [ ] `testdata/` містить ≥8 невалідних фікстур — мінімум по одній на кожен код помилки; unit-тест на кожну перевіряє, що повертається саме очікуваний код.
  - [ ] Всі `examples/` проходять валідацію без помилок.
  - [ ] Файл із двома незалежними помилками повертає обидві.
  - [ ] Фікстура з циклічним `details` (A → B → A): валідація завершується (не зависає), exit 0, кожен файл перевірено один раз; помилка всередині B репортиться рівно один раз.

### Крок 1.3 — CLI `dc validate`
`cmd/dc`: команда `dc validate <files...>` (підтримка glob). Вивід: людиночитний список помилок `file:line [DCxxx] message`; прапорець `--json` для машинного виводу. Exit code 0 = валідно, 1 = помилки валідації, 2 = помилка виконання (файл не знайдено тощо).
- **AC:**
  - [ ] `go build -o dc ./cmd/dc` збирає бінарник.
  - [ ] `./dc validate examples/*.dc.yaml` → exit 0, вивід містить summary (скільки файлів OK).
  - [ ] `./dc validate testdata/invalid_flow_no_link.dc.yaml` → exit 1, вивід містить `DC004` і номер рядка.
  - [ ] `./dc validate nonexistent.yaml` → exit 2.
  - [ ] `./dc validate --json ...` видає валідний JSON (перевірити `| python3 -m json.tool`).

### Крок 1.4 — CI репозиторію
GitHub Actions: на PR/push — `go build`, `go test`, `go vet`, `./dc validate examples/*.dc.yaml`.
- **AC:**
  - [ ] `.github/workflows/ci.yml` існує і локальна симуляція кроків (ті самі команди в shell) проходить.

---

# Фаза 2 — Генератор AI-контексту

**Мета:** `dc context file.dc.yaml` → markdown-опис для вставки в AI-чат.

### Крок 2.1 — Генератор
`internal/context`: з валідної моделі генерує markdown: заголовок+purpose+audience → секція Components (тип, label, description, ai_context) → секція Links (людською мовою, з label) → по секції на кожен Flow (нумеровані кроки з note, branch як відступні під-списки) → якщо є `details`-референси, дописати "Node X has a detailed sub-diagram: <path>" і за прапорцем `--deep` інлайнити під-діаграму. `--deep` обходить референси з множиною відвіданих файлів: кожна під-діаграма інлайниться один раз, повторний/циклічний референс дає лише рядок-згадку без інлайну.
- **AC:**
  - [ ] Golden-тести: `testdata/golden/*.md` — згенерований вивід для кожного example побайтово збігається з golden-файлом (оновлення через `go test -update`).
  - [ ] Вивід для `auth-system` містить всі 5 вузлів, всі links, обидва flows, і жодного вузла/зв'язку, якого немає у джерелі.
  - [ ] Невалідний файл → команда відмовляється генерувати, exit 1, показує помилки валідації.

### Крок 2.2 — CLI-інтеграція
`dc context <file> [-o out.md] [--deep]`; без `-o` — у stdout.
- **AC:**
  - [ ] `./dc context examples/auth-system.dc.yaml -o /tmp/ctx.md` створює файл, exit 0.
  - [ ] `--deep` на файлі з референсом включає вміст під-діаграми (перевірити наявність вузла з oauth-detail у виводі).

---

# Фаза 3 — Рендеринг через D2/Mermaid + CI-інтеграція

**Мета:** картинки для слайдів без власного рендерера. D2 — основний шлях (нативна Go-бібліотека), Mermaid — текстовий експорт.

### Крок 3.1 — Транспілятор у D2-текст
`internal/transpile`: модель → D2-рядок. Мапінг типів на shapes (actor→person, storage→cylinder, queue→queue, service→rectangle, external→rectangle зі стилем). Labels на ребрах.
- **AC:**
  - [ ] `./dc export examples/auth-system.dc.yaml --to d2` друкує D2-текст; збережений у файл, він компілюється офіційним d2 без помилок (у тесті — через Go-бібліотеку `d2lib.Compile`).
  - [ ] Golden-тести на D2-вивід усіх examples.

### Крок 3.2 — Транспілятор у Mermaid
Аналогічно, `--to mermaid` (flowchart TD). Це деградований експорт: без гарантій стилів, для вставки в GitHub/Notion.
- **AC:**
  - [ ] Golden-тести; вивід починається з `flowchart`, містить усі вузли та ребра.
  - [ ] Вивід рендериться `mmdc` (mermaid-cli) без помилок — якщо `mmdc` недоступний у середовищі, AC замінюється на перевірку синтаксису парсером у CI позначкою TODO.

### Крок 3.3 — Нативний рендер SVG
`internal/render`: через `d2lib` + layout dagre/elk → `dc render <file> -o out.svg [--layout dagre|elk] [--theme <id>]`. PNG — конвертацією SVG (`--format png`, через d2 playwright-less шлях або rsvg; якщо складно — PNG відкласти, зафіксувати в README).
- **AC:**
  - [ ] `./dc render examples/auth-system.dc.yaml -o /tmp/auth.svg` → валідний SVG (починається з `<svg` або XML-пролога, містить текст "Gateway").
  - [ ] Обидва layout-движки дають SVG без помилок.

### Крок 3.4 — Flow: статичне підсвічування
`dc render --flow "Успішна авторизація"`: вузли і ребра зі шляху flow отримують акцентний стиль (колір/товщина) у згенерованому D2, решта — приглушені.
- **AC:**
  - [ ] SVG для flow відрізняється від базового; ребра шляху мають інший stroke, ніж ребра поза шляхом (перевірити атрибути в SVG).
  - [ ] Неіснуюча назва flow → exit 1 з переліком доступних flows.

### Крок 3.5 — Flow: покрокова серія кадрів
`dc render --flow X --steps -o dir/`: N файлів `step-01.svg … step-NN.svg`; кадр i підсвічує кроки 1..i, поточний крок — найяскравіший. Branch: кожна гілка — окрема під-послідовність (`step-03a-…`, `step-03b-…`).
- **AC:**
  - [ ] Для flow з 6 кроків створюється рівно 6 файлів (плюс гілки, якщо є), всі — валідні SVG.
  - [ ] Layout стабільний між кадрами: координати вузла `Gateway` однакові в усіх кадрах (порівняти transform/x у SVG).

### Крок 3.6 — Анімований SVG (D2 steps)
`dc render --flow X --animate -o out.svg`: транспілювати кроки в D2 composition/steps і зібрати з animate-interval.
- **AC:**
  - [ ] Вивід — один SVG, що містить анімаційні елементи (`<animate` або відповідний D2-механізм), відкривається у браузері без помилок (ручна/скріншот-перевірка допустима).

### Крок 3.7 — Позначка вузлів із під-діаграмою (`details`)
Вузли, що мають `details`-референс, візуально маркуються у всіх експортах: у D2/SVG — суфікс/іконка в label (напр. ` ⊞`) + окремий стиль (подвійна рамка або клас `has-details`); у SVG додати `<a href>` на шлях під-діаграми (відносний, з заміною `.dc.yaml` → `.svg`), щоб у браузері вузол був клікабельним; у Mermaid — `click`-директива або суфікс у label (деградований експорт).
- **AC:**
  - [ ] SVG для `auth-system` містить маркер на вузлі з `details` і не містить його на вузлах без `details` (перевірити атрибути/текст у SVG).
  - [ ] Вузол з `details` у SVG обгорнутий у посилання на відповідний файл під-діаграми.
  - [ ] Golden-тести D2/Mermaid оновлено; `dc render` без `details` у файлі не змінює вивід (регресія на прикладах без референсів).

### Крок 3.8 — Готовий CI-рецепт для користувачів
`docs/ci.md` + приклад `.github/workflows/diagrams.example.yml`: validate → render SVG → context → upload artifacts. Реліз бінарника: goreleaser-конфіг (linux/mac/windows, amd64+arm64).
- **AC:**
  - [ ] `goreleaser build --snapshot --clean` збирає бінарники для ≥4 платформ.
  - [ ] docs/ci.md містить повний робочий workflow, що використовує лише команди, реалізовані в попередніх кроках.

---

# Фаза 4 — Layout-файл і стабільні позиції

**Мета:** ручні позиції вузлів, що переживають ре-рендер (міст до web-редактора).

### Крок 4.1 — Формат layout
`docs/format.md` доповнити: `<name>.layout.json` = `{ "views": { "<view>": { "positions": { "<nodeId>": {"x":.., "y":..} } } } }`. Node id, відсутні в layout, лягають автолейаутом; id в layout, відсутні в ядрі — warning, не помилка.
- **AC:**
  - [ ] Формат задокументовано; JSON Schema для layout додано в `schema/`.

### Крок 4.2 — Рендер з урахуванням layout
`dc render` автоматично підхоплює `<name>.layout.json` поруч із ядром (або `--layout-file`); D2 підтримує фіксовані позиції через `top`/`left` — використати їх.
- **AC:**
  - [ ] Тест: заданий у layout вузол опиняється у вказаних координатах у SVG (±5px).
  - [ ] `dc render --write-layout` після автолейауту зберігає обчислені позиції у layout-файл; повторний рендер дає ідентичний SVG (детермінізм).

---

# Фаза 5 — Web-редактор (TypeScript)

**Мета:** інтерактив: flow-плеєр, drag&drop, кілька view. Окремий застосунок у `web/`, споживає ті самі файли.

### Крок 5.1 — Go→WASM валідатор
Зібрати `validate`+`parser` у WASM (`GOOS=js GOARCH=wasm`), експортувати `validate(yamlString) -> errors[]`.
- **AC:**
  - [ ] `web/public/dc.wasm` збирається make-таргетом; JS-тест (node + wasm_exec.js) валідує рядок з examples → 0 помилок, зіпсований рядок → DC004.

### Крок 5.2 — Скелет застосунку
Vite + React + TypeScript у `web/`. Відкриття `.dc.yaml` (file input / drag файлу), парсинг (js-yaml) + валідація через WASM, рендер графу: elkjs для layout + SVG-компоненти.
- **AC:**
  - [ ] `npm run build` без помилок; `npm test` (vitest) зелений.
  - [ ] Відкриття `auth-system.dc.yaml` показує всі 5 вузлів і всі ребра (playwright-тест).

### Крок 5.3 — Drag & drop → layout.json
Перетягування вузла змінює лише позиції; кнопка "Export layout" скачує `<name>.layout.json` у форматі фази 4; імпорт layout-файлу відновлює позиції.
- **AC:**
  - [ ] Playwright: перетягнути вузол → експортований JSON містить нові координати; ядро-YAML не змінюється (застосунок його взагалі не переписує).
  - [ ] Експортований layout, підкладений до CLI `dc render`, дає ті самі позиції (крос-перевірка з фазою 4).

### Крок 5.4 — Flow-плеєр
Вибір flow зі списку → контроли: prev/next/autoplay. Активний крок: підсвічене ребро + анімація руху маркера по ребру (`<animateMotion>`), панель з `note` поточного кроку. Branch відображається як вибір гілки користувачем.
- **AC:**
  - [ ] Playwright: вибір flow і 3× "next" → підсвічено перші 3 кроки, панель показує note 3-го кроку.
  - [ ] Autoplay проходить flow до кінця і зупиняється.

### Крок 5.5 — Drill-down навігація по `details`
Вузол із `details`-референсом має видимий індикатор (іконка ⊞ / стиль); подвійний клік (або кнопка в панелі вузла) відкриває під-діаграму в тому ж редакторі. Стек переходів + breadcrumbs угорі ("auth-system › oauth-detail"), клік по крихті повертає на відповідний рівень зі збереженням стану (позиції, вибраний flow). Під-діаграма читається відносно шляху батьківського файлу; помилка завантаження (файл відсутній/невалідний) показується як нефатальне повідомлення, без падіння застосунку.
- **AC:**
  - [ ] Playwright: відкрити `auth-system.dc.yaml` → вузол з `details` має індикатор, вузли без — ні.
  - [ ] Playwright: подвійний клік по вузлу з `details` → рендеряться вузли `oauth-detail`, breadcrumbs показує обидва рівні; клік по "auth-system" повертає верхню схему з тими самими позиціями вузлів.
  - [ ] Битий `details`-шлях → повідомлення про помилку, редактор лишається на поточній схемі.

### Крок 5.6 — Експорти
Кнопки: PNG поточного кадру, серія PNG всіх кроків flow (zip), AI-context (markdown, та сама логіка що CLI — допустимо викликати через WASM).
- **AC:**
  - [ ] PNG скачується і непорожній; markdown збігається з виводом CLI `dc context` для того ж файлу.

---

# Фаза 6 — Движок канви: міграція на React Flow

**Мета:** замінити самописний SVG-в'ювер на React Flow (`@xyflow/react`) без втрати функціоналу фази 5 — фундамент для повноцінного редагування у фазі 7. Міграція покроково: обидва рендерери можуть співіснувати за feature-флагом до кроку 6.5.

### Крок 6.1 — Базовий рендер на React Flow
Додати `@xyflow/react`; кастомні node-компоненти для кожного типу (`actor, service, storage, queue, external, component`) з тим самим візуальним кодуванням, що у фазі 5; кастомний edge з label та типом link; minimap, controls (zoom/fit), pan/zoom з коробки.
- **AC:**
  - [ ] Відкриття `auth-system.dc.yaml` рендерить всі 5 вузлів і всі ребра на React Flow-канві (playwright-тест, адаптований з 5.2).
  - [ ] Кожен із 6 базових типів вузлів має візуально відмінний кастомний компонент (unit-тест: рендер ноди кожного типу дає відмінний CSS-клас/data-атрибут).
  - [ ] `npm test` + `npm run build` зелені.

### Крок 6.2 — Auto-layout (elkjs) + ручні позиції
elkjs рахує стартові позиції; drag вузла зберігає позицію в тому ж форматі layout (фаза 4); імпорт/експорт `<name>.layout.json` працює як у кроці 5.3; кнопка "Re-layout" перераховує автолейаут для вузлів без ручної позиції.
- **AC:**
  - [ ] Playwright: перетягнути вузол → "Export layout" → JSON містить нові координати; ядро-YAML не змінено.
  - [ ] Імпорт layout-файлу з фази 4 відновлює позиції на React Flow-канві (крос-перевірка: той самий файл, що проходив AC 5.3).
  - [ ] "Re-layout" не чіпає вузли з ручними позиціями (тест: вузол із layout-файлу лишається на місці ±1px).

### Крок 6.3 — Flow-плеєр на React Flow
Перенести flow-плеєр (крок 5.4): підсвічування пройдених ребер через стан edge-компонента, маркер руху по активному ребру (анімація вздовж edge path), панель note, вибір branch, prev/next/autoplay.
- **AC:**
  - [ ] Playwright: вибір flow і 3× "next" → перші 3 ребра мають підсвічений стан (data-атрибут/клас), панель показує note 3-го кроку.
  - [ ] Autoplay проходить flow до кінця і зупиняється; branch пропонує вибір гілки.

### Крок 6.4 — Drill-down по `details` на React Flow
Перенести крок 5.5: індикатор ⊞ на кастомній ноді, подвійний клік відкриває під-діаграму, breadcrumbs, збереження стану рівня при поверненні, нефатальна помилка на битий шлях.
- **AC:**
  - [ ] Проходять ті самі три AC, що у кроці 5.5, на React Flow-канві.

### Крок 6.5 — Видалення старого рендерера
Видалити самописний SVG-в'ювер (`DiagramView` старого зразка, layout-код, що дублюється) і feature-флаг; експорти (5.6) переведені на нову канву (PNG через знімок viewport React Flow або серіалізацію SVG).
- **AC:**
  - [ ] У `web/src` не лишилось мертвого коду старого рендерера (grep за старими компонентами порожній).
  - [ ] Всі AC кроку 5.6 (PNG, zip кроків flow, AI-context markdown) проходять на новій канві.
  - [ ] Повна регресія: всі playwright-тести фаз 5–6 зелені; `npm test`, `npm run build` зелені.

---

# Фаза 7 — Повноцінне візуальне редагування

**Мета:** редагувати діаграму (nodes/links/flows) прямо на канві; кожна візуальна дія — патч до YAML; YAML лишається джерелом правди і синхронний із канвою в обидва боки.

### Крок 7.1 — Серіалізація: модель → YAML-патчі
Модуль `web/src/yamlPatch.ts` на бібліотеці `yaml` (eemeli/yaml): операції `addNode, updateNode, removeNode, addLink, removeLink, addFlowStep, removeFlowStep, renameNodeId` застосовуються до YAML-документа зі збереженням коментарів, порядку ключів і форматування незмінених частин.
- **AC:**
  - [ ] Unit-тести на кожну операцію: патч валідного документа дає документ, що парситься і містить зміну.
  - [ ] Документ з коментарями: після `addNode` + `removeLink` всі коментарі та порядок незмінених секцій збережені побайтово (golden-тест).
  - [ ] `renameNodeId` оновлює всі згадки id у links і flows.

### Крок 7.2 — CRUD вузлів з канви
Палітра типів (перетягнути тип на канву → нова нода з auto-id + модалка id/label); подвійний клік/панель властивостей — редагування label, type, description, tags; delete — видалення з підтвердженням, якщо є залежні links/flow-кроки (каскадне видалення або відмова — показати список залежностей).
- **AC:**
  - [ ] Playwright: додати ноду з палітри → вона на канві, у YAML-стані з'явився блок `nodes[]` з валідним id.
  - [ ] Playwright: перейменувати label через панель → YAML оновлено, канва показує новий label.
  - [ ] Видалення вузла з залежними links показує попередження з переліком; підтвердження видаляє вузол разом із залежними links.

### Крок 7.3 — Малювання links + інспектор зв'язків
Хендли на нодах: протягнути з ноди на ноду → модалка type/label → новий link у YAML. Правий сайдбар "Links": список всіх зв'язків із фільтром за типом і за вузлом; hover елемента списку підсвічує ребро на канві, hover ребра підсвічує елемент списку; клік — редагування/видалення link.
- **AC:**
  - [ ] Playwright: протягнути з'єднання між двома нодами → у YAML-стані новий елемент `links[]` з правильними from/to.
  - [ ] Створити link можна лише між існуючими нодами (спроба кинути з'єднання в порожнечу не створює нічого).
  - [ ] Playwright: hover по елементу списку links → відповідне ребро отримує highlight-клас; видалення зі списку прибирає ребро з канви і з YAML.

### Крок 7.4 — Редактор flows
Режим "запис flow": кліки по ребрах у порядку сценарію додають кроки (з полем note); клік по ребру, якого нема — неможливий (реберна дисципліна формату гарантується UI); підтримка додавання branch (умова + запис then/else гілок); редагування/видалення кроків списком у сайдбарі.
- **AC:**
  - [ ] Playwright: записати flow з 3 кроків кліками по ребрах → YAML містить flow з 3 steps з правильними from/to.
  - [ ] Кожен записаний крок відповідає існуючому link (валідація після запису — 0 помилок DC004).
  - [ ] Записаний flow одразу програється flow-плеєром фази 6.

### Крок 7.5 — YAML-панель з двосторонньою синхронізацією
Бічна панель з CodeMirror 6 (yaml-режим): правка тексту → перепарсинг → оновлення канви (debounce); візуальна правка → патч (7.1) → оновлення тексту без втрати позиції курсора поза зміненим діапазоном. Некоректний YAML у панелі не ламає канву — показується остання валідна версія + індикатор помилки.
- **AC:**
  - [ ] Playwright: дописати ноду в текст панелі → нода з'являється на канві без перезавантаження.
  - [ ] Playwright: додати ноду з палітри → текст у панелі містить нову ноду, коментарі користувача на місці.
  - [ ] Ввести синтаксично битий YAML → канва показує попередній стан, панель показує маркер помилки з рядком.

### Крок 7.6 — Live-валідація на канві
Кожна зміна (з канви чи з YAML-панелі) → WASM-валідація (debounce) → маркери: нода/ребро з помилкою отримує error-стиль + tooltip з `DCxxx` і повідомленням; панель "Problems" зі списком всіх помилок, клік — фокус на елементі канви або рядку YAML.
- **AC:**
  - [ ] Playwright: видалити link, який використовує flow (через YAML-панель) → відповідний flow позначено помилкою DC004 у панелі Problems без натискання кнопок.
  - [ ] Клік по помилці в Problems центрує канву на проблемному елементі (або курсор на рядку YAML для помилок без візуального елемента).
  - [ ] Валідний документ → панель Problems порожня, індикатор "OK".

### Крок 7.7 — Undo/Redo
Єдина історія для всіх мутацій (канва + YAML-панель) на рівні YAML-документа; Ctrl/Cmd+Z / Shift+Z; ліміт ≥50 кроків.
- **AC:**
  - [ ] Playwright: додати ноду → undo → ноди нема ні на канві, ні в YAML; redo повертає.
  - [ ] Правка в YAML-панелі і візуальна правка відкочуються в правильному порядку одною історією.

---

# Фаза 8 — Публічний сайт

**Мета:** редактором можуть користуватися сторонні люди: відкрити/зберегти свої файли, поділитися лінком, стартувати з прикладів. Сервер не потрібен — все клієнтське (WASM-валідація), деплой статичний.

### Крок 8.1 — Відкриття та збереження файлів
File System Access API: "Open" (`.dc.yaml` + автопідхоплення `<name>.layout.json` поруч), "Save"/"Save as" пише зміни назад у файли (ядро і layout окремо). Fallback для браузерів без FS API (Firefox/Safari): file input + download. Індикатор незбережених змін; попередження beforeunload.
- **AC:**
  - [ ] Playwright (Chromium): відкрити файл, додати ноду, зберегти → файл на диску містить нову ноду; layout зберігся окремим файлом.
  - [ ] У браузері без FS API кнопки деградують до import/download без падінь (перевірка через підміну `window.showOpenFilePicker`).
  - [ ] Незбережені зміни → індикатор у заголовку і спроба закрити вкладку викликає попередження.

### Крок 8.2 — Share-лінки
"Share": стан (ядро-YAML + layout) стискається (fflate) → base64url у fragment (`#s=...`); відкриття такого URL відновлює діаграму read-write (як незбережений документ). Ліміт розміру з людським повідомленням, якщо діаграма завелика для URL.
- **AC:**
  - [ ] Playwright: створити діаграму → Share → відкрити отриманий URL у новому контексті → та сама діаграма з тими самими позиціями.
  - [ ] Fragment не летить на сервер (стан лише після `#`); URL для `auth-system` ≤ 8KB.

### Крок 8.3 — Галерея прикладів і онбординг
Стартовий екран: "New diagram", "Open file", галерея прикладів (усі з `examples/`, вбудовані в бандл) з прев'ю; короткий вбудований tour (3–5 підказок: палітра, links, flow-плеєр, валідація).
- **AC:**
  - [ ] Playwright: клік по прикладу з галереї відкриває його в редакторі, всі вузли на місці.
  - [ ] Прев'ю прикладів генеруються білд-скриптом з реальних файлів `examples/` (не рукою намальовані картинки).

### Крок 8.4 — Деплой і CI
Статичний деплой (GitHub Pages або Cloudflare Pages — вирішити при виконанні, зафіксувати в deviations якщо GH Pages): GitHub Actions збирає WASM + `npm run build` + playwright і публікує на push у main. `web/README.md` переписати під проєкт (замінити Vite-шаблон).
- **AC:**
  - [ ] Workflow-файл існує; локальна симуляція його кроків (ті самі команди) проходить.
  - [ ] Продакшн-білд відкривається з file-серверa (`vite preview`) і WASM-валідація працює (playwright проти preview).
  - [ ] Сайт доступний за публічним URL, відкриває приклад із галереї (ручна перевірка, URL зафіксувати в README).

---

# Фаза 9 — MCP-сервер і AI-стиль

**Мета:** агент (Claude Code та ін.) може створити діаграму, провалідувати, відрендерити, побачити результат і виправити — через MCP, у консистентному стилі.

### Крок 9.1 — `dc mcp`: базові інструменти
Команда `dc mcp` (stdio-транспорт, офіційний Go MCP SDK): інструменти `validate_diagram(path|content)`, `get_context(path, deep)`, `list_diagrams(dir)` — тонкі обгортки над `internal/validate`, `internal/context`. Помилки валідації повертаються структуровано (code, file, line, message).
- **AC:**
  - [ ] Інтеграційний тест: MCP-клієнт (Go SDK testing client) викликає `validate_diagram` на `examples/auth-system.dc.yaml` → 0 помилок; на фікстурі з DC004 → помилка з кодом і рядком.
  - [ ] `dc mcp` заведений у Claude Code (`claude mcp add`) — ручна перевірка, команду підключення записати в `docs/mcp.md`.
  - [ ] `go test ./... && go vet ./...` зелені.

### Крок 9.2 — Структуровані операції редагування
Інструмент `edit_diagram(path, operations[])` з тим самим набором операцій, що у кроці 7.1 (`add_node, update_node, remove_node, add_link, remove_link, add_flow_step, remove_flow_step, rename_node_id, set_position`): Go-реалізація патчить YAML зі збереженням коментарів (yaml.v3 Node-рівень); `set_position` пише в layout-файл. Кожен виклик: застосувати → провалідувати → якщо валідація впала, відкотити і повернути помилки (атомарність).
- **AC:**
  - [ ] Тест: `add_node` + `add_link` через MCP → файл містить зміни, коментарі збережені (golden).
  - [ ] Операція, що ламає валідність (link на неіснуючий вузол), НЕ змінює файл і повертає структуровану помилку DC002.
  - [ ] `set_position` змінює лише layout-файл, ядро незаймане.

### Крок 9.3 — Style guide і `lint_style`
Формат `<dir>/dc-style.yaml`: конвенції для набору діаграм (дозволені типи/теги, патерни іменування id, обов'язковість description, ліміт вузлів на діаграму, тема рендера). Інструмент MCP `get_style_guide(dir)` (агент читає перед малюванням) і `lint_style(path)` (перевірка відповідності, коди `DS0xx`); те саме доступно як `dc lint --style`.
- **AC:**
  - [ ] Фікстури: діаграма, що порушує ≥3 правила прикладного style-файлу → `lint_style` повертає всі порушення з кодами; відповідна діаграма → 0.
  - [ ] `docs/format.md` доповнено специфікацією `dc-style.yaml`; приклад style-файлу лежить у `examples/`.
  - [ ] `dc lint --style` дає той самий результат, що MCP-інструмент (спільна реалізація, тест на обидва шляхи).

### Крок 9.4 — Рендер для "очей" агента
Інструмент `render_diagram(path, flow?, format=svg|png)` повертає зображення (MCP image content) — агент бачить результат без браузера. PNG — через існуючий шлях фази 3 (якщо PNG був відкладений — SVG обов'язковий, PNG best-effort, зафіксувати в deviations).
- **AC:**
  - [ ] Інтеграційний тест: `render_diagram` на `auth-system` повертає image-контент, непорожній, валідний SVG/PNG.
  - [ ] Виклик з `flow` повертає зображення з підсвіченим шляхом (відрізняється від базового).
  - [ ] `docs/mcp.md` описує всі інструменти з прикладом повного циклу агента: створити → провалідувати → відрендерити → виправити.

---

# Правила для агента-валідатора

1. Прогнати всі AC кроку як чекліст; AC з командами виконувати буквально і звіряти exit code/вивід.
2. Регресія: перед закриттям кроку — `go test ./...`, `go vet ./...`, `./dc validate examples/*.dc.yaml` (з фази 1 і далі); у web/ — `npm test` + `npm run build` (з фази 5).
3. Крок не закривається, якщо для нового коду немає тестів, передбачених AC.
4. Розбіжність плану з реальністю (API D2 змінився, залежність недоступна) — не мовчки обходити: зафіксувати відхилення у `docs/deviations.md` (крок, причина, прийняте рішення) і продовжити.
5. Питання, що змінюють зафіксовані рішення (формат, мова, розділення layout) — ескалювати власнику, не вирішувати самостійно.
